@model List<string>

@{
    Layout = null;
}

<div class="well">
    <h2>Usuarios: @Model.Count</h2>

    <ul>
        @foreach (var name in Model)
        {
            <li>@name</li>
        }
    </ul>
</div>
